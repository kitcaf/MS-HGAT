PAD = 0  # 填充符号的索引值 --- 在数据预处理中用于将不同长度的序列填充到相同长度
       # PAD在user_size计算中算作一个特殊用户，实际上它不是真实用户
       # 批量训练时需保持输入序列长度一致，短序列用PAD填充到最大长度
EOS = 1  # 序列结束符号的索引值 --- 在数据预处理中标记序列的结束
       # EOS也在user_size计算中算作一个特殊用户，所以user_size = 实际用户数 + 2
       # 在自回归生成（如机器翻译、文本生成）中，模型遇到EOS停止输出

step_split = 8  # 时间步长划分数量，用于将扩散过程分成多个时间段
n_heads = 14  # 注意力头的数量，用于多头注意力机制

# 预测步长，用于多步预测
STEP_SHIFT = 3  # 预测未来3步的用户
